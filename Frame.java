package bombermanfinal;

import java.awt.BorderLayout;
import javax.swing.JFrame;

/**
 *
 * @author faurehu
 */
public class Frame extends JFrame {
    
    //Map map;
    //Info info;
    Game game;
    
    public Frame(Game g) {
        game = g;
        //this.setLayout(new BorderLayout());
        //this.setSize(400,400);
        //map = new Map(g);
        //map.addKeyListener(new Key());
        //info = new Info(g);
        //map.add(info);
        //this.add(map);
        //this.add(info);
        //this.pack();

    }
    
}